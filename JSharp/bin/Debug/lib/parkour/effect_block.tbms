{"projectName":"effect_block","datapackDirectory":null,"offuscate":false,"isLibrary":false,"files":[{"name":"import","content":"\n","index":0},{"name":"main","content":"package effect_block\n\ndef lazy effectBlock(int $block, int $effect, int $dur, int $amp){\n\tforgenerate($i,-0.3,0.3,0.3){\n\t\tforgenerate($j,-0.3,0.3,0.3){\n\t\t\tif (block(~$i ~-1 ~$j $block)){\n\t\t\t\t/effect give @s $effect $dur $amp true\n\t\t\t}\n\t\t}\n\t}\n}\n\ndef lazy effectBlockCMD(int $block, string $cmd){\n\tforgenerate($i,-0.3,0.3,0.3){\n\t\tforgenerate($j,-0.3,0.3,0.3){\n\t\t\tif (block(~$i ~-0.1 ~$j $block)){\n\t\t\t\tpositioned(~$i ~-0.1 ~$j){\n\t\t\t\t\t/$cmd\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n\ndef lazy effectBlockFun(int $block, string $cmd){\n\tforgenerate($i,-0.3,0.3,0.3){\n\t\tforgenerate($j,-0.3,0.3,0.3){\n\t\t\tif (block(~$i ~-0.1 ~$j $block)){\n\t\t\t\tpositioned(~$i ~-0.1 ~$j){\n\t\t\t\t\t$cmd()\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n\ndef lazy effectBlockHead(int $block, int $effect, int $dur, int $amp){\n\tforgenerate($i,-0.3,0.3,0.3){\n\t\tforgenerate($j,-0.3,0.3,0.3){\n\t\t\tif (block(~$i ~1.9 ~$j $block)){\n\t\t\t\t/effect give @s $effect $dur $amp true\n\t\t\t}\n\t\t}\n\t}\n}\n\ndef lazy effectBlockHeadCMD(int $block, string $cmd){\n\tforgenerate($i,-0.3,0.3,0.3){\n\t\tforgenerate($j,-0.3,0.3,0.3){\n\t\t\tif (block(~$i ~1.9 ~$j $block)){\n\t\t\t\tpositioned(~$i ~1.9 ~$j){\n\t\t\t\t\t/$cmd\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n\ndef lazy effectBlockHeadFun(int $block, string $cmd){\n\tforgenerate($i,-0.3,0.3,0.3){\n\t\tforgenerate($j,-0.3,0.3,0.3){\n\t\t\tif (block(~$i ~1.9 ~$j $block)){\n\t\t\t\tpositioned(~$i ~1.9 ~$j){\n\t\t\t\t\t$cmd()\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","index":0},{"name":"single","content":"package effect_block\n\ndef lazy effectBlockSingle(int $block, int $effect, int $dur, int $amp){\n\tif (block(~ ~-1 ~ $block)){\n\t\t/effect give @s $effect $dur $amp true\n\t}\n}\n\ndef lazy effectBlockSingleCMD(int $block, string $cmd){\n\tif (block(~ ~-1 ~ $block)){\n\t\t/$cmd\n\t}\n}\n\ndef lazy effectBlockSingleFun(int $block, string $cmd){\n\tif (block(~ ~-1 ~ $block)){\n\t\t$cmd()\n\t}\n}\n\ndef lazy effectBlockSingleHead(int $block, int $effect, int $dur, int $amp){\n\tif (block(~ ~1.9 ~ $block)){\n\t\t/effect give @s $effect $dur $amp true\n\t}\n}\n\ndef lazy effectBlockSingleHeadCMD(int $block, string $cmd){\n\tif (block(~ ~1.9 ~ $block)){\n\t\t/$cmd\n\t}\n}\n\ndef lazy effectBlockSingleHeadFun(int $block, string $cmd){\n\tif (block(~ ~1.9 ~ $block)){\n\t\t$cmd()\n\t}\n}\n\n","index":0}],"TagsList":{"blocks":{},"functions":{}},"mcTagsList":{"blocks":{},"functions":{"load":{"values":["load"]},"tick":{"values":["main"]}}},"compileOrder":[],"version":{"major":1,"minor":0,"patch":0,"build":8}}