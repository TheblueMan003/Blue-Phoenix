{"projectName":"raycast","datapackDirectory":null,"resourcesPackDirectory":null,"offuscate":true,"isLibrary":true,"description":null,"files":[{"name":"import","content":"import standard.java\r\nimport standard.entity_id\r\n\r\nimport tools","index":0},{"name":"raycast","content":"package raycast\r\n\r\ndef lazy shoot(float distance, int $precision, int $stopCondtion, function $action){\r\n    float dis = distance\r\n    tag(shooter)\r\n    def innerLoop(float dist){\r\n        if ($stopCondtion){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else if (dist <= 0){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else{\r\n            positioned(^ ^ ^$precision){\r\n                /tp @s ~ ~ ~\r\n                innerLoop(dist-$precision)\r\n            }\r\n        }\r\n    }\r\n    tools.summonPointer(\"raycast\")\r\n    /tp @e[tag=raycast] ~ ~1.5 ~ ~ ~\r\n    with(@e[tag=raycast],true){\r\n        innerLoop(dis)\r\n    }\r\n}\r\n\r\ndef lazy shoot(float distance, int $precision, function $action){\r\n    float dis = distance\r\n    tag(shooter)\r\n    def innerLoop(float dist){\r\n        if (!block(~ ~ ~ air)){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else if (dist <= 0){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else{\r\n            positioned(^ ^ ^$precision){\r\n                /tp @s ~ ~ ~\r\n                innerLoop(dist-$precision)\r\n            }\r\n        }\r\n    }\r\n    tools.summonPointer(\"raycast\")\r\n    /tp @e[tag=raycast] ~ ~1.5 ~ ~ ~\r\n    with(@e[tag=raycast],true){\r\n        innerLoop(dis)\r\n    }\r\n}\r\n\r\ndef lazy shoot(float distance, function $action){\r\n    float dis = distance\r\n    tag(shooter)\r\n    def innerLoop(float dist){\r\n        if (!block(~ ~ ~ air)){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else if (dist <= 0){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else{\r\n            positioned(^ ^ ^0.5){\r\n                /tp @s ~ ~ ~\r\n                innerLoop(dist-0.5)\r\n            }\r\n        }\r\n    }\r\n    tools.summonPointer(\"raycast\")\r\n    /tp @e[tag=raycast] ~ ~1.5 ~ ~ ~\r\n    with(@e[tag=raycast],true){\r\n        innerLoop(dis)\r\n    }\r\n}","index":0},{"name":"laser","content":"package raycast\r\n\r\ndef lazy laser(float distance, int $precision, int $stopCondtion, function $stepAction, function $action){\r\n    float dis = distance\r\n    tag(shooter)\r\n    def innerLoop(float dist){\r\n        if ($stopCondtion){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else if (dist <= 0){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else{\r\n            with(@e[tag=shooter]){\r\n                $stepAction()\r\n            }\r\n            positioned(^ ^ ^$precision){\r\n                /tp @s ~ ~ ~\r\n                innerLoop(dist-$precision)\r\n            }\r\n        }\r\n    }\r\n    tools.summonPointer(\"raycast\")\r\n    /tp @e[tag=raycast] ~ ~1.5 ~ ~ ~\r\n    with(@e[tag=raycast],true){\r\n        innerLoop(dis)\r\n    }\r\n}\r\n\r\ndef lazy laser(float distance, int $precision, function $stepAction, function $action){\r\n    float dis\r\n    tag(shooter)\r\n    def innerLoop(float dist){\r\n        if (!block(~ ~ ~ air)){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else if (dist <= 0){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else{\r\n            with(@e[tag=shooter]){\r\n                $stepAction()\r\n            }\r\n            positioned(^ ^ ^$precision){\r\n                /tp @s ~ ~ ~\r\n                innerLoop(dist-$precision)\r\n            }\r\n        }\r\n    }\r\n    tools.summonPointer(\"raycast\")\r\n    /tp @e[tag=raycast] ~ ~1.5 ~ ~ ~\r\n    with(@e[tag=raycast],true){\r\n        innerLoop(dis)\r\n    }\r\n}\r\n\r\ndef lazy laser(float distance, function $stepAction, function $action){\r\n    float dis\r\n    tag(shooter)\r\n    def innerLoop(float dist){\r\n        if (!block(~ ~ ~ air)){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else if (dist <= 0){\r\n            with(@e[tag=shooter]){\r\n                $action()\r\n                untag(shooter)\r\n            }\r\n            kill(@s)\r\n        }\r\n        else{\r\n            with(@e[tag=shooter]){\r\n                $stepAction()\r\n            }\r\n            positioned(^ ^ ^0.5){\r\n                /tp @s ~ ~ ~\r\n                innerLoop(dist-0.5)\r\n            }\r\n        }\r\n    }\r\n    tools.summonPointer(\"raycast\")\r\n    /tp @e[tag=raycast] ~ ~1.5 ~ ~ ~\r\n    with(@e[tag=raycast],true){\r\n        innerLoop(dis)\r\n    }\r\n}\r\n","index":0}],"resources":[],"TagsList":{"blocks":{},"functions":{}},"mcTagsList":{"blocks":{},"functions":{"load":{"values":["load"]},"tick":{"values":["main"]}}},"compileOrder":[],"version":{"major":1,"minor":0,"patch":0,"build":4},"compilationSetting":{"TreeMaxSize":20,"FloatPrecision":1000,"removeUselessFile":true,"offuscate":true,"scoreboardValue":"tbms.value","scoreboardConst":"tbms.const","scoreboardTmp":"tbms.tmp","tagsFolder":true,"forcedOffuscation":{},"libraryFolder":["./lib/1_16_5/","./lib/shared/"],"MCVersion":"1.17","ExportAsZip":false,"packformat":6,"rppackformat":7,"CompilerCoreName":"java","Authors":"","generateMAPSFile":true,"generateREADMEFile":true,"opti_FunctionTagsReplace":true,"opti_ExportComment":false,"isLibrary":false}}